on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - parallel:
          - run: echo one
          - run: echo two
      - id: server
        run: echo server
        background: true
      - wait: server
      - wait-all:
      - cancel: server
